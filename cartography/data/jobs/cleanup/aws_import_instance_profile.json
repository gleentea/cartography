{
  "statements": [{
    "query": "MATCH (:AWSRole) -[r:AWS_ATTACHED_ROLE]->(:AWSInstanceProfile) WHERE r.lastupdated <> {UPDATE_TAG} WITH r LIMIT {LIMIT_SIZE} DELETE (r) return COUNT(*) as TotalCompleted",
    "iterative": true,
    "iterationsize": 100
  }],
  "name": "cleanup AWSInstanceProfile AWS_ATTACHED_ROLE"
}
